<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.rj.hyhealthbackend.mapper.MaterialMapper">

    <!--查询所有的必备材料-->
    <select id="getAllMaterial" resultType="cn.rj.hyhealthbackend.model.MaterialModel">
        SELECT id,title,message FROM `material`
        <where>
            <trim prefixOverrides="OR">
                <if test="keyword != null and keyword != ''">
                    <bind name="key" value="'%' + keyword + '%'"/>
                    OR id LIKE #{key}
                    OR title LIKE #{key}
                    OR message LIKE #{key}
                </if>
            </trim>
        </where>
    </select>
</mapper>